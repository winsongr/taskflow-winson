info:
  name: List Projects
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/projects
  body:
    type: none
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
